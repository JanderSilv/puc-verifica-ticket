'use client'
import { styled } from '@mui/material/styles'
import LottiePlayer, { LottieComponentProps } from 'lottie-react'

export type LottieProps = LottieComponentProps
export const Lottie = styled(LottiePlayer)``
